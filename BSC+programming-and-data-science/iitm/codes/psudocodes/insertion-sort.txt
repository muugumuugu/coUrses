procedure sorted_list_insert(sortedL,item){
	newL=[]
	inserted=false
	foreach el in sortedL{
		if (not inserted){ # compare elements if item has not been inserted yet
			if (item < el) {# add item before the element in list if it is smaller.
			newList = newList ++ [x]
			inserted = True
			}
		}
		newL = newL ++ [el] # add the existing elements in order.
		if (not inserted){# if item is larger than all items and has hence not been inserted, add it to the end
			newL++[item]
		}
	}
}

procedure InsertionSort(l)
	sortedList = [] # initialize an empty, (hence sorted) list
	foreach el in l {
		sortedList =SortedListInsert(sortedlList,el) # insert in a sorted fashion each element from the original list into this second list
	}
	return(sortedList)
End InsertionSort

```